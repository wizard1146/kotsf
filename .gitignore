# OS / editor cruft
.DS_Store
*.swp
Thumbs.db

# Node
node_modules/
npm-debug.log*

# Local env / scratch
.env
.env.local
*.log

# Assets: track only our own art (icons + splash); the rest are borrowed
# placeholders from another project — keep them out of history for now.
assets/*
!assets/icons/
!assets/splash.png
